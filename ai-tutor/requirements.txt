# AI Tutor — Virtual TA dependencies
# Phase 5: Python FastAPI microservice for Socratic hint generation
# using OpenAI GPT-4o + RAG pipeline (Watanobe lab EDM research)

fastapi==0.111.0
uvicorn[standard]==0.30.1

# OpenAI GPT-4o — Generative AI engine for the Virtual TA
openai==1.35.0

# PostgreSQL — fetch submission code + problem context; write ai_hint_text + cognitive_effort_index
psycopg[binary,pool]==3.1.19

# Redis — Pub/Sub: publish hint events to submissions.events.<user_id>
# websocket-service subscribes and pushes hints to the student browser in real time
redis==5.0.7

# Pydantic v2 — request/response validation and Settings management
pydantic==2.7.4
pydantic-settings==2.3.4

# HTTP client — used for inter-service calls if needed
httpx==0.27.0
